language: cpp

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq inkscape
    - sudo apt-get install -qq doxygen

install: python ./config/travis/installDependencies.py

script: scons build CXX=$CXX

compiler:
    - gcc
    - clang
